apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx
  name: svc-lb-nginx
  namespace: strigus
spec:
  ports:
  - nodePort: 32333
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: nginx
  sessionAffinity: None
  type: LoadBalancer
